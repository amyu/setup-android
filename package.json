{
  "name": "setup-android",
  "version": "5",
  "description": "setup-android for self hosted runner",
  "type": "module",
  "main": "dist/setup/index.js",
  "packageManager": "pnpm@11.11.0",
  "scripts": {
    "postinstall": "patch-package",
    "build": "tsc",
    "lint": "biome check ./src/",
    "lint-fix": "biome check --write --unsafe ./src/",
    "typecheck": "tsc --project tsconfig.json --pretty --noEmit --incremental false",
    "package": "pnpm dlx rimraf ./dist && pnpm run typecheck && rolldown --config rolldown.config.js",
    "all": "pnpm run postinstall && pnpm run lint && pnpm run package"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/actions/typescript-action.git"
  },
  "keywords": [
    "setup-android",
    "actions",
    "android",
    "setup",
    "self",
    "hosted",
    "runner"
  ],
  "author": "amyu",
  "license": "MIT",
  "dependencies": {
    "@actions/cache": "^6.1.0",
    "@actions/core": "^3.0.0",
    "@actions/exec": "^3.0.0",
    "@actions/io": "^3.0.0",
    "@actions/tool-cache": "^4.0.0"
  },
  "devDependencies": {
    "@biomejs/biome": "^2.0.0",
    "@types/node": "^24.4.0",
    "patch-package": "8.0.1",
    "rolldown": "^1.2.0",
    "typescript": "^7.0.0"
  },
  "engines": {
    "node": ">=24.0.0"
  }
}
